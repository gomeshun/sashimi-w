# q10 adoption control

Full arrays, exported-source archive hashes, constructor settings, dependency
versions, warnings and exact runner bytes are in the family repository:
`validation/science/sashimi-w/q10-before-09322fe` and `q10-adopted-v2`.
`comparison.json` records both report hashes and the 21 array checks per mass.
The controls were evaluated in separate processes under the same core source
and environment. See the companion family `execution-provenance.json` for the
editable core distribution-metadata caveat; its actual source SHA was verified.
